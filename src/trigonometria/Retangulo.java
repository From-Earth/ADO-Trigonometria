package trigonometria;

public class Retangulo extends Trigonometria {
	public double area(Retangulo forma) {
		double calculo;
		
		
		calculo = (this.getBase()*this.getAltura());
		
		return calculo;
		
}
}